activityplan:
  rules:
    bookingConfirmed:
      tasks:
        - initializeActivityPlan
        - bookingSubmitted
        - bookingConfirmed
        - emailEvent
    bookingUpdated:
      tasks:
        - bookingUpdated
    bookingRejected:
      tasks:
        - bookingRejected
        - emailEvent